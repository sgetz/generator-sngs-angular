language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-sngs-angular' ]; then cd .. && eval "mv $currentfolder generator-sngs-angular" && cd generator-sngs-angular; fi

